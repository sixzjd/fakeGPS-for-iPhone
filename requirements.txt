# Pinned: 11.14.0 silently changes what gets bundled (~1 MB smaller on macOS),
# so an unpinned range turns every rebuild into an untested device-layer change.
# Bump deliberately, and re-run the --selftest CI guard plus a real-device check.
pymobiledevice3==11.12.5

# For GUI mode
pywebview==5.4
pythonnet==3.0.5
clr-loader==0.2.7.post0
